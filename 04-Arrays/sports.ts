let typesOfSports: string[] = ["Golf", "Cricket", "Tennis", "Swimming"];

typesOfSports.forEach(element => {
    console.log(element);
});


// simplified for loop
for (let temp of typesOfSports) {
    console.log("Simplified version of for loop " + temp);
}